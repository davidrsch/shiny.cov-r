% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{report}
\alias{report}
\title{Generate a coverage report}
\usage{
report(x, file = "coverage-report/index.html", app_dir = ".", ...)
}
\arguments{
\item{x}{A coverage object returned by \code{\link[=collect]{collect()}}.}

\item{file}{Output \code{.html} path (default \code{"coverage-report/index.html"};
dependencies are written to a \verb{lib/} directory alongside it).}

\item{app_dir}{Path to the app directory for the UI manifest and
interaction log. Defaults to \code{"."}.}

\item{...}{Ignored (kept for compatibility).}
}
\value{
The HTML report path (invisibly).
}
\description{
Produces a covr-style HTML report with a per-file overview (\code{Files} tab)
and a per-file source view (\code{Source} tab). In the source view the hit
count for every line is annotated with its per-test-source breakdown
(\verb{2 (cypress=1 shinytest2=1)}) whenever the run was tagged with
\code{SHINYCOV_SOURCE}. \code{x} already has UI interaction coverage merged
directly into it (see \code{\link[=merge_ui_coverage]{merge_ui_coverage()}}), so
\code{\link[covr:percent_coverage]{covr::percent_coverage()}} and this report are one blended number.
}
