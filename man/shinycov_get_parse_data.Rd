% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trace.R
\name{shinycov_get_parse_data}
\alias{shinycov_get_parse_data}
\title{Parse data for a srcfile, for \code{shinycov_impute_srcref()}'s use}
\usage{
shinycov_get_parse_data(srcfile)
}
\description{
covr's own \code{get_parse_data()} additionally supports \verb{#line} directives
(via the \code{rex} package plus a small file-content cache) -- a feature
for preprocessed/generated source files (e.g. Rcpp attributes output)
that remap positions back to an original, different file. shiny.cov
only ever instruments plain \code{.R} files it parses itself, which never
contain \verb{#line} directives, so that whole layer is dead code here --
dropped rather than carried over, to avoid a new dependency (\code{rex})
for a code path that can't be reached from this package's own usage.
}
\details{
Cached directly on \code{srcfile} itself: every top-level srcref belonging to
one parsed file shares a single, mutable srcfile environment (already
relied on elsewhere -- see \code{box_hook.R}'s own mutation of \code{sf$filename}
for the same object), and every \code{parse()} call allocates a brand-new
srcfile object, even for the same file path parsed twice. So caching
here is scoped exactly right with no separate cache-lifecycle/key
bookkeeping needed: reused for every \code{shinycov_impute_srcref()} call
made while tracing this one parse (can otherwise mean re-parsing the
whole file from scratch once per un-braced \code{if}/\code{for}/\code{while}/\code{switch}
branch encountered), and it can never go stale across a different
\code{instrument_file()} call or a file's contents changing, since those
always get their own fresh srcfile object from a fresh \code{parse()}.
}
\keyword{internal}
